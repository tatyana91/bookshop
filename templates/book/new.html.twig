{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block container %}
    <h1>{{ 'Create new Book'|trans }}</h1>

    {{ include('book/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('book_index') }}">{{ 'back to list'|trans }}</a>
{% endblock %}
